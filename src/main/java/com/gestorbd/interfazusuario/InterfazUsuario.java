package com.gestorbd.interfazusuario;

/**
 * Paquete reservado para la interfaz de usuario del gestor de base de datos.
 *
 * Aquí se implementará la vista gráfica (o de consola) que permita al usuario
 * interactuar con las operaciones CRUD del motor:
 *   - Insertar documentos
 *   - Buscar por ID o por campo
 *   - Actualizar documentos existentes
 *   - Eliminar documentos
 *   - Visualizar el árbol AVL
 *
 * La interfaz se comunicará exclusivamente con {@link com.gestorbd.motor.GestorBaseDatos}
 * y no accederá directamente ni al árbol ni a la capa de persistencia.
 */
public class InterfazUsuario {
    // Por ahora vacío — implementación pendiente
}
